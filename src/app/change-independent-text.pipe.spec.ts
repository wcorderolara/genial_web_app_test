import { ChangeIndependentTextPipe } from './change-independent-text.pipe';

describe('ChangeIndependentTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeIndependentTextPipe();
    expect(pipe).toBeTruthy();
  });
});
